{% extends 'layout-admin.html.twig' %}

{% block page_title %}{{ 'Reference documents'|trans|page_title }}{{ 'Meetings'|trans|page_title }}{{ parent() }}{% endblock %}

{% block admin_breadcrumbs %}
    <li class="breadcrumb-item">
        <a href="{{ path('admin/meetings/index') }}">{{ 'Meetings'|trans }}</a>
    </li>
    <li class="breadcrumb-item active" aria-current="page">{{ 'Reference documents'|trans }}</li>
{% endblock %}

{% block contents %}
    <div class="d-flex flex-wrap justify-content-between align-items-center gap-2 mb-3">
        <h2 class="mb-0">{{ 'Reference documents'|trans }}</h2>
    </div>

    {{ component('Decision:Admin:ReferenceLibrary') }}

    {% include 'partials/application/confirm-modal.html.twig' %}
{% endblock %}
