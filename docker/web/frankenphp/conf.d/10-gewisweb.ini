expose_php = 0
date.timezone = Europe/Amsterdam
apc.enable_cli = 1
session.use_strict_mode = 1
zend.detect_unicode = 0

; GEWISWEB specifics
memory_limit = 512M
max_input_time = 60
max_execution_time = 45

; Allow php-vips (libvips via FFI) to load at request time. The default `preload` would block the synchronous
; generate-on-miss image encode in the FrankenPHP worker; variant pre-generation itself runs in CLI messenger workers.
ffi.enable = true

upload_max_filesize = 64M
post_max_size = 192M
max_input_vars = 15000

short_open_tag = 0

report_memleaks = 1
register_argc_argv = 0
auto_globals_jit = 1

output_buffering = 4096
cli_server.color = 1

; https://symfony.com/doc/current/performance.html
realpath_cache_size = 4096K
realpath_cache_ttl = 600
opcache.interned_strings_buffer = 16
opcache.max_accelerated_files = 32531
opcache.memory_consumption = 256
opcache.enable_file_override = 1
