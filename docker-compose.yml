version: '2'

services:
  nginx:
    image: koen1999/gewisweb_nginx:latest
    depends_on:
        -   web
    networks:
        - network
    ports:
        - "8010:80"
    restart: unless-stopped

  web:
    image: koen1999/gewisweb_web:latest
    volumes:
        - code:/code
    networks:
        - network
    restart: unless-stopped

volumes:
  code:
      external:
          name: gewisweb

networks:
    network:
        external:
            name: gewisweb
