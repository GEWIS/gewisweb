version: '2'

services:
  nginx:
    image: nginx:latest
    build: docker/nginx
    depends_on:
        -   web
    ports:
        - "80:80"

  web:
    build: .
    volumes:
      - code:/code

volumes:
  code:
      external:
          name: gewisweb
