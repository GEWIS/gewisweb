<?php
$this->headTitle($this->translate('Create Job Label'));

$this->headScript()
    ->appendFile($this->basePath() . '/js/company-create.js');

$form->prepare();
$form->setAttribute('class', 'form-company');
?>
<section class="section">
    <div class="container">
        <div class="row">
            <div class="col-md-12">
                <h1><?= $this->translate('Create Job Label') ?></h1>
            </div>
        </div>
        <hr>
        <?= $this->form()->openTag($form) ?>
        <?= $this->partial(
            'partial/company/admin/editors/label.phtml',
            [
                'form' => $form,
            ],
        ) ?>
        <div class="row">
            <div class="col-md-12">
                <?php
                $submit = $form->get('submit')
                    ->setValue($this->translate('Create Job Label'))
                    ->setAttribute('class', 'btn btn-primary pull-right');
                ?>
                <div class="form-group">
                    <?= $this->formSubmit($submit) ?>
                </div>
            </div>
        </div>
        <?= $this->form()->closeTag() ?>
    </div>
</section>
<script>
    Company.updateForm();
</script>

